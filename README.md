# Steam Friends' Leaderboard 🏆

Сервис для пользователей Steam, который позволяет сравнивать игровую статистику (в первую очередь, время в играх) с друзьями. Сервис анализирует библиотеку игр пользователя и его друзей, после чего формирует "таблицы лидеров" по конкретным играм.

⚠️ **Важно**: Сервис может анализировать только **публичные** профили Steam, у которых в настройках приватности открыт доступ к списку игр и списку друзей.

**🔗 Ссылка на рабочий проект:** [https://xrystik.pythonanywhere.com](https://xrystik.pythonanywhere.com)

## 🛠️ Технологии
*   **Backend**: Python 3.12, Django 6.0, django REST framework
*   **Frontend**: Tailwind CSS, DaisyUI, Lucide
*   **База данных**: SQLite
*   **Интеграции**: Steam Web API
*   **Инструменты**: pip, npm

## 📸 Демонстрация
*Главная страница, где пользователь вводит ссылку на свой профиль:*
![Главная страница]()

*Страница профиля с таблицей лидеров по одной из игр:*
![Страница профиля]()

## ⚙️ Как запустить проект локально

**Важно**: Перед первым запуском убедитесь, что у вас установлен Python 3.12+, а также Node.js и npm.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ваш-юзернейм/ваш-репо.git
    cd ваш-репо
    ```

2.  **Настройте Backend (Python):**
    *   Создайте и активируйте виртуальное окружение:
        ```bash
        python -m venv venv
        # Windows
        venv\Scripts\activate
        # macOS/Linux
        source venv/bin/activate
        ```
    *   Установите зависимости:
        ```bash
        pip install -r requirements.txt
        ```

3.  **Настройте Frontend (CSS):**
    *   Перейдите в директорию с фронтенд-ресурсами:
        ```bash
        cd theme/static_src
        ```
    *   Установите npm-зависимости:
        ```bash
        npm install
        ```
    *   Скомпилируйте CSS-файлы:
        ```bash
        npm run build
        ```
    *   Вернитесь в корневую директорию проекта:
        ```bash
        cd ../..
        ```

4.  **Настройте переменные окружения:**
    *   Создайте файл `.env` в корневой директории проекта.
    *   Добавьте в него следующие переменные, заменив значения на свои:
        ```
        SECRET_KEY='ваш-секретный-ключ-django'
        STEAM_API_KEY='ваш-ключ-steam-api'
        ```

5. **Примените миграции базы данных:**

    ```bash

    python manage.py migrate

    ```



6. **Создайте суперпользователя для доступа в админку:**

    ```bash

    python manage.py createsuperuser

    ```



7. **Запустите сервер для разработки:**

    ```bash

    python manage.py runserver

    ```



8. **Откройте проект в браузере:**

    *   Главная страница: [http://127.0.0.1:8000/](http://127.0.0.1:8000/)

    *   Админ-панель: [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/)


